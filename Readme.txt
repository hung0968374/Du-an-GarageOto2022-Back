To start project run:

- npm run dev

With every variable you create in env:

- Write the same key in .env.example then add it on Doppler

Insert data to database with file

- Within every update you make (on creating a new table, change row, ...) update file database.sql then note it down for team to know
- To insert to database. Open MySQL then import file sql to MySQL. Voila the data's your